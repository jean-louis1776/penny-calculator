import React from "react"
import { Navbar } from "react-bootstrap"

const Header = (props) => {
    return (
        <Navbar>
            <Navbar.Text>Калькулятор пенни по 44-ФЗ</Navbar.Text>
        </Navbar>
    )
}

export default Header
